# Latest public data

No governed scientific dataset is published here yet.

The first release is withheld until the current public-metric reconciliation corrections pass the private scientific gates and this repository's independent release validation.

When populated, this directory will contain selected machine-readable public-safe evidence summaries with explicit units, count units, coverage, quality state, provenance and source/reuse information.
