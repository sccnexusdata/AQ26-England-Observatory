# Public provenance

This directory will contain only provenance receipts approved for public release, such as runtime/environment summaries, release identifiers, source-catalogue summaries and reproducibility metadata.

It will not contain credentials, protected logs, raw deployment configuration or provider-restricted material.

The authoritative public file list and SHA-256 hashes are recorded in `PUBLIC_RELEASE_MANIFEST.json`.
